/*import { CreateItemService } from "../../config/database/createItemService";

class Controller {
  handle(req, res) {
    const createItemService = new CreateItemService();
    const result = createItemService.execute(body);
    return res.json(result);
  }
}

export default Controller;*/
